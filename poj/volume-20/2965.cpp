#include <stdio.h>

void solve(int start, int end){
}

int main(){
    int cnt = 0, beg = 0, end = ; 
    while((c = getchar()) && (c == '+' || c == '-')){
        if(c == '+')
            beg |= (1<<cnt);
        ++ cnt;
        solve(beg, 0);
    }
    return 0 ;
}
